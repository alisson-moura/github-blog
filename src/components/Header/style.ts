import styled from "styled-components";

export const HeaderContainer = styled.header`
    width: 100vw;
    height: 18.5rem;
    background: #0B1E30;
    display: grid;
    grid-template-columns: repeat(3, 1fr);
`

export const Logo = styled.img`
    justify-self: center;
    margin-top: 4rem;
`

export const EffectLeft = styled.img`
    justify-self: start;
    margin-top: 4.3rem;
`;

export const EffecRight = styled.img`
    justify-self: end;
    margin-top: 2rem;
`